const mainButton = document.getElementById ("btn-list")

mainButton.addEventListener("click, toggleText")

function toggleText(event){
    this.classList.toggle("display")
} //tentative de début de Javascript, mais pas le temps !//